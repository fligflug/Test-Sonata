{% extends '@SonataAdmin/CRUD/base_list_field.html.twig' %}
{% block field %}
    <div>
        {% if object.image != null %}
            <img src="{{ asset(blog_images ~ object.image) }}">
        {% else %}
            <span>No picture</span>
        {% endif %}
    </div>
{% endblock %}
